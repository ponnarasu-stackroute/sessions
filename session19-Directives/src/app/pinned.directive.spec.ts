import { PinnedDirective } from './pinned.directive';

describe('PinnedDirective', () => {
  it('should create an instance', () => {
    const directive = new PinnedDirective();
    expect(directive).toBeTruthy();
  });
});
